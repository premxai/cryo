worker_processes auto;
error_log /var/log/nginx/error.log warn;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent"';
    access_log /var/log/nginx/access.log main;

    sendfile        on;
    keepalive_timeout 65;
    gzip on;
    gzip_types text/plain application/json application/javascript text/css;

    limit_req_zone $binary_remote_addr zone=api:10m rate=30r/m;
    limit_req_zone $binary_remote_addr zone=v1:10m rate=120r/m;

    # Plain HTTP — redirect everything to HTTPS except the ACME challenge path
    # (certbot's renewal hook needs this reachable over :80 to renew in place).
    server {
        listen 80;
        server_name api.cryoweb.xyz;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        http2 on;
        server_name api.cryoweb.xyz;

        ssl_certificate     /etc/letsencrypt/live/api.cryoweb.xyz/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/api.cryoweb.xyz/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Authenticated SaaS API — app enforces per-key limits; this zone is a
        # coarse per-IP safety net. Long read timeout: Wayback live fetches and
        # cold CDX list-domain queries run 20-75s; /v1/answer up to ~30s.
        location /v1/ {
            limit_req zone=v1 burst=30 nodelay;
            proxy_pass http://127.0.0.1:8000/v1/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 90s;
        }

        # Public playground proxy (strict app-level per-IP limits)
        location /demo/ {
            limit_req zone=v1 burst=10 nodelay;
            proxy_pass http://127.0.0.1:8000/demo/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 90s;
        }

        # MCP streamable HTTP — no buffering, long-lived requests
        location /mcp {
            limit_req zone=v1 burst=30 nodelay;
            proxy_pass http://127.0.0.1:8000/mcp;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_read_timeout 300s;
            proxy_http_version 1.1;
        }

        location /api/ {
            limit_req zone=api burst=10 nodelay;
            proxy_pass http://127.0.0.1:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 30s;
        }

        location /healthz/ {
            proxy_pass http://127.0.0.1:8000/healthz/;
            proxy_set_header Host $host;
        }

        location / {
            proxy_pass http://127.0.0.1:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
